apps:
  - script: ./appclustering.js
    name: "hockshi_cluster"
    watch: true
    env:
      NODE_ENV: development
    env_production:
      NODE_ENV: production
    env_test:
      NODE_ENV: test
